# Tasks: Arquitetura modular do Worker

Ordem: types → lib/ → routes/ → esvaziar index.ts → script novo → instruções → verificação. Leovio delega uma por vez; nenhuma muda comportamento de API.

- [ ] **T1 [worker]** Criar `platform/worker/src/types.ts` com `Env` + todos os payload interfaces hoje espalhados no topo de `index.ts` (linhas 28-114, mais `AdminUserRow` e `NotaUpdatePayload` do meio do arquivo). — @platform-api-worker — checkpoint: `index.ts` importa de `./types` em vez de declarar localmente; nada mais muda ainda.
- [ ] **T2 [worker]** Criar `platform/worker/src/lib/jwt.ts` (`importKey`, `signJwt`, `verifyJwt`) e `lib/crypto.ts` (`safeEqual`, `pbkdf2`, `hashPassword`, `verifyPassword`, `sha256Hex`, `randomToken`). — @platform-api-worker — depende de: T1 — checkpoint: `index.ts` importa dos dois módulos novos.
- [ ] **T3 [worker]** Criar `platform/worker/src/lib/http.ts` (`corsHeaders`, `jsonResponse`) e `lib/email.ts` (`sendEmail`, `sendPasswordResetEmail`). — @platform-api-worker — depende de: T1 — checkpoint: importável, sem duplicar corpo.
- [ ] **T4 [worker]** Criar `platform/worker/src/lib/auth.ts` (`requireAuth`, `requireAdmin`, `adminJwt`, `isAllowedOrigin`, `isForgotPasswordAllowed`). — @platform-api-worker — depende de: T2, T3 — checkpoint: importa `signJwt`/`verifyJwt`/`jsonResponse` dos módulos já extraídos, sem duplicar.
- [ ] **T5 [worker]** Criar `platform/worker/src/lib/turma-context.ts` (`resolveTurmaParam`, `ANO_CURSO_ATUAL`). — @platform-api-worker — depende de: T1 — checkpoint: importável por painel/grade/boletim.
- [ ] **T6 [worker]** Criar `routes/auth.ts` (login, forgot/reset password, google callback admin+aluno) e `routes/sync.ts` (`handleSync`). — @platform-api-worker — depende de: T4 — checkpoint: handlers idênticos, só relocados + imports ajustados.
- [ ] **T7 [worker]** Criar `routes/entregas.ts` (create/get entregas + entregas-historico admin) e `routes/message.ts` (get/put mensagem do site). — @platform-api-worker — depende de: T4 — checkpoint: idem.
- [ ] **T8 [worker]** Criar `routes/calendar.ts` (get/import calendar, resumo-ha). — @platform-api-worker — depende de: T4 — checkpoint: idem.
- [ ] **T9 [worker]** Criar `routes/seed.ts` (`handleAdminSeed`, sem mudar a lógica de upsert por seção). — @platform-api-worker — depende de: T4 — checkpoint: idem; comentário no topo do arquivo aponta pra constitution §9 como a exceção documentada.
- [ ] **T10 [worker]** Criar `routes/avaliacoes.ts` (get avaliacoes, novidade, marcar-vistas + `resolveAvaliacoesTurmaId` local), `routes/turmas.ts` e `routes/ucs.ts`. — @platform-api-worker — depende de: T4 — checkpoint: idem.
- [ ] **T11 [worker]** Criar `routes/painel.ts`, `routes/notas.ts`, `routes/boletim.ts`, `routes/grade.ts`, todos importando `lib/turma-context.ts`. — @platform-api-worker — depende de: T5 — checkpoint: idem, sem duplicar `resolveTurmaParam`/`ANO_CURSO_ATUAL`.
- [ ] **T12 [worker]** Esvaziar `index.ts`: remove todo corpo de handler/helper já extraído, deixa só imports + o dispatcher (`if`/`switch` sobre `url.pathname`+método) + `export default { fetch }`. — @platform-api-worker — depende de: T6, T7, T8, T9, T10, T11 — checkpoint: `index.ts` fica com ~100-150 linhas; nenhuma URL/método/branch de roteamento muda de ordem ou comportamento.
- [ ] **T13 [script]** Criar `platform/scripts/publish-avaliacao.mjs <slug>`: lê só `avaliacoes/<slug>/meta.yaml`, monta payload mínimo (`avaliacoes` + `avaliacaoIndicadores` daquele slug), POST em `/api/admin/seed` (mesmo endpoint, payload parcial); sem `--post` imprime o payload (dry-run). — @platform-api-worker — depende de: — — checkpoint: CA4 do spec.md (payload de teste só tem av07 + vínculo, nada de ucs/indicadores/turmas).
- [ ] **T14 [docs]** Atualizar `.claude/agents/platform-api-worker.md`, `.claude/skills/platform-api-worker/SKILL.md` e `.claude/skills/platform-contexto/SKILL.md`: remover o mandato de arquivo único, descrever a estrutura `routes/`+`lib/`+`types.ts`, atualizar contagem de rotas. — @platform-agent (Leovio, edita specs/skills) — depende de: T12 — checkpoint: CA5 do spec.md.
- [ ] **T15 [docs]** Adicionar item de checklist em `.claude/agents/platform-agent.md` (Leovio) e `.claude/agents/platform-qa.md`: verificar, antes de aprovar uma mudança, se um arquivo de rota está crescendo demais ou se um endpoint está virando genérico/multi-entidade. — @platform-agent — depende de: T12.
- [ ] **T16 [docs]** Sincronizar `.github/agents/`: reescrever `platform-agent.agent.md` e criar os 5 arquivos faltantes (`platform-api-worker`, `platform-schema-d1`, `platform-componentes-vue`, `platform-ui-ux`, `platform-qa`), espelhando o conteúdo atualizado dos `.claude/agents/` equivalentes. — @platform-agent — depende de: T14, T15 — checkpoint: CA6 do spec.md.
- [ ] **T17 [qa]** Type-check (`tsc --noEmit` ou equivalente) + smoke test manual (login, `GET /api/avaliacoes`, `POST /api/admin/seed` parcial, `PUT /api/admin/notas`) + conferir que nenhum arquivo novo passa de ~200 linhas. — @platform-qa — depende de: T12, T13 — checkpoint: CA1, CA2, CA3 do spec.md.
