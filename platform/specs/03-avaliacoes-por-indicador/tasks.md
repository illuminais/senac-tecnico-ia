# Tasks: Avaliações por trimestre e indicador

Ordem: saneamento → shared → schema → seed → API → Vue → script → test → QA. Leovio delega uma por vez.

Dividida em duas fases. **A fase 2 não começa antes da fase 1 estar verde no QA.**

> Revisão 2026-07-28 (turma): T1, T3, T4, T5, T6, T7, T8, T9, T10 ajustadas pra dimensão **turma** (ver [[dec-turma-dimensao]] e o plan.md revisado). Nenhuma task foi renumerada — CA/task references do analyze.md continuam válidas.
> Revisão 2026-07-28 (conhecimentos/habilidades, D9): T1, T3, T4, T6, T8, T9, T12 ajustadas de novo — UC vira tabela própria (`ucs`), fonte do seed passa a ser `contextos/conteudo-base/ucs-elementos-da-competencia.md` + `distribuicao-trimestral-ano1.md` (não mais `contextos/indicadores.md`, que segue existindo só como tracker de cobertura real).

---

## Fase 1 — estrutura e lançamento de nota (T1–T9, T12, T13)

O valor central: dado saneado, aluno vendo trimestre/indicador/UC, professor vendo cobertura e lançando A/PA/NA.

- [ ] **T1 [dados]** Consertar `parseYaml` em `build-all.mjs` pra entender lista YAML (inline `[A, B]` e multilinha `- A`); desfazer os documentos YAML duplicados de `av04`/`av05`/`av06` mantendo título e `status` reais; adicionar `trimestre` e `indicadores: [UCxx.N]` aos 6 `meta.yaml`. **Também**: criar `contextos/turmas.md` (tabela `id | ano_ingresso | status`, uma linha `2026A | 2026 | ativa`). Não precisa mais restruturar `contextos/indicadores.md` — a fonte de UCs/indicadores/conhecimentos/habilidades agora é `contextos/conteudo-base/ucs-elementos-da-competencia.md` (já estruturada por UC) + `distribuicao-trimestral-ano1.md` (trimestres); `indicadores.md` segue existindo só como tracker de cobertura real, fora do seed. — @platform-api-worker — checkpoint: `avaliacoes.json` sai com `ucs` populado e títulos corretos (CA1); `turmas.md` existe no formato novo.
- [ ] **T2 [shared]** `consolidarNota`, `parseIndicadorCodigo`, `formatIndicadorCodigo`, `toCsvRow` em `platform/shared/pure.ts`. — @platform-api-worker — checkpoint: importáveis por worker e portal.
- [ ] **T3 [schema]** 7 tabelas (`ucs`, `turmas`, `indicadores` referenciando `ucs.codigo`, `avaliacoes` sem prazo, `avaliacao_indicadores`, `avaliacoes_turma`, `notas`) + 5 índices em `schema.sql` conforme plan revisado; `CREATE TABLE IF NOT EXISTS`, idempotente. **Também**: o `ALTER TABLE users ADD COLUMN turma_id ...` de uma vez só (não idempotente — ver plan.md, seção "primeira quebra da promessa"), documentado como comando manual pro professor rodar antes de reaplicar o resto. — @platform-schema-d1 — depende de: —— — checkpoint: `schema.sql` aplica em D1 local sem erro, reaplica sem erro (exceto o `ALTER TABLE`, que é rodado uma vez só, fora do arquivo idempotente).
- [ ] **T4 [seed]** `platform/scripts/seed-indicadores.mjs`: parseia `contextos/conteudo-base/ucs-elementos-da-competencia.md` (9 UCs → `ucs`, com `ano_curso`/conhecimentos/habilidades; 55 indicadores → `indicadores`, código+descrição+uc) + `contextos/conteudo-base/distribuicao-trimestral-ano1.md` (trimestres por indicador) + `contextos/turmas.md` + os `meta.yaml` → JSON no formato de `POST /api/admin/seed`, incluindo `ucs`, `turmas` e `avaliacoes_turma` (broadcast do prazo/status do `meta.yaml` pra toda turma `status='ativa'`). Segue o padrão de `seed-calendar.mjs` (stdout ou `--post`). — @platform-api-worker — depende de: T1, T2 — checkpoint: `UC05` sai com `ano_curso: 1` e conhecimentos/habilidades não vazios; `UC01.3` sai com `trimestres: 'T1,T2,T3'`; `2026A` sai como turma ativa com `avaliacoes_turma` populada.
- [ ] **T5 [api]** `POST /api/admin/seed` (JWT admin, upsert em lote de **ucs**/indicadores/avaliações/vínculos/turmas/avaliacoes_turma, **nunca** toca `notas` nem `users.turma_id`) + `GET /api/avaliacoes` (pública, JWT de aluno opcional pra resolver turma via `avaliacoes_turma`, fallback pra turma ativa única quando sem sessão — ver plan.md). — @platform-api-worker — depende de: T3, T4 — checkpoint: seed 2× ⇒ estado idêntico e notas intactas (CA5).
- [ ] **T6 [api]** `GET /api/admin/turmas` (lista pro seletor) + `GET /api/admin/ucs` (referência estática, RF19) + `GET /api/admin/painel?trimestre&turma`, `GET /api/admin/grade/:slug?turma`, `PUT /api/admin/notas` (batch, `valor: null` apaga), `GET /api/admin/boletim?trimestre&turma`. `turma` opcional com fallback só se houver exatamente uma ativa; senão `400`. — @platform-api-worker — depende de: T5 — checkpoint: painel do T2 lista indicador sem avaliação (CA2); boletim aplica `max` (CA3); dois alunos de turmas diferentes nunca aparecem na mesma grade; `GET /api/admin/ucs` traz conhecimentos/habilidades de UC05 (CA10).
- [ ] **T7 [vue]** `AvaliacoesView` passa a consumir `GET /api/avaliacoes`, agrupa por trimestre, mostra indicador + UC, com loading/erro/vazio. Nenhuma nota na tela. — @platform-componentes-vue — depende de: T5 — checkpoint: CA6.
- [ ] **T8 [vue]** `AdminPainelView` (`/admin/avaliacoes`) — seletor de trimestre **+ componente `SeletorTurma.vue` novo** (via `GET /api/admin/turmas`, oculto quando só existe uma), lista por indicador agrupada por UC (filtrada por `ano_curso`), destaque "sem avaliação", contador de corrigidos. Indicador expansível mostra conhecimentos/habilidades da UC (busca `GET /api/admin/ucs` uma vez, resolve local por `indicador.uc` — RF19). — @platform-componentes-vue — depende de: T6.
- [ ] **T9 [vue]** `AdminGradeView` (`/admin/avaliacoes/:slug`) — grade aluno × indicador **de uma turma** (usa `SeletorTurma.vue` de T8 ou recebe via rota), teclado (`A`/`P`/`N` marca e desce, setas navegam, `Backspace` limpa), "marcar coluna toda", estado sujo + salvar explícito, coluna de entregou. Cabeçalho de coluna expansível, mesmo mecanismo de `GET /api/admin/ucs` do painel (RF19). — @platform-componentes-vue — depende de: T6, T8 — checkpoint: CA4 (lote não cria NA implícito); CA10.
- [ ] **T12 [test]** Property tests de `consolidarNota` (ordem-invariância, idempotência, máximo), roundtrip de `parseIndicadorCodigo`; exemplo de `parseUcsMd` contra `ucs-elementos-da-competencia.md` real (9 UCs, `ano_curso` e conhecimentos/habilidades não vazios); exemplo de `parseIndicadoresMd`+trimestres contra o arquivo real (todo `indicador.uc` resolve numa `ucs.codigo`); exemplo de `parseTurmasMd` contra `contextos/turmas.md`. — @platform-componentes-vue — depende de: T2, T4.
- [ ] **T13 [qa]** `npm run build` + `npm run test` + `wrangler deploy --dry-run` + revisão de convenções. Idempotência do seed e integridade de `notas` verificadas contra D1 local (incluindo `turmas`/`avaliacoes_turma`); confirmar que o `ALTER TABLE` manual de T3 está documentado como comando pronto, não executado por nenhum agente. — @platform-qa — depende de: T7..T9, T12.

---

## Fase 2 — fechamento de trimestre (T10, T11, T14, T15)

Só dói no fim do trimestre. Não bloqueia nada da fase 1.

- [ ] **T10 [vue]** `AdminBoletimView` (`/admin/boletim`) — matriz aluno × indicador consolidada **de uma turma** (`SeletorTurma.vue` de T8) + export CSV via `toCsvRow`. — @platform-componentes-vue — depende de: T6, T2, T8 — checkpoint: CA7 (nome com vírgula não desalinha).
- [ ] **T11 [script]** `platform/scripts/dump-indicadores.mjs <trimestre> [turma]` — lê o D1 e regrava `contextos/indicadores-t{N}.md` no formato atual (schema `indicadores-trimestre`, status `⬜/🔄/✅` + coluna Avaliação); `turma` opcional com o mesmo fallback de T6. Rodado à mão, diff revisável. — @platform-api-worker — depende de: T6 — checkpoint: regravar o T2 sem notas lançadas produz o arquivo atual sem diff espúrio.
- [ ] **T14 [test]** Property test do roundtrip de `toCsvRow` (vírgula, aspas e quebra de linha não mudam a contagem de colunas). — @platform-componentes-vue — depende de: T2, T10.
- [ ] **T15 [qa]** `npm run build` + `npm run test` + revisão. Export CSV conferido abrindo o arquivo numa planilha de verdade. — @platform-qa — depende de: T10, T11, T14.
